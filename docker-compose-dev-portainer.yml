version: '3.8'

services:
  app-dev:
    image: lostinvirtual-dev:latest
    container_name: lostinvirtual-app-dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    restart: always
