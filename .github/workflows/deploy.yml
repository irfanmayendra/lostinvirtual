name: Deploy Pipeline
on:
  push:
    branches: [main, staging, develop]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to VPS
        run: |
          # SSH to VPS and trigger update script
          # (Logic to trigger update on VPS via Portainer API or Webhook)
          echo "Deploying branch ${{ github.ref_name }}..."
